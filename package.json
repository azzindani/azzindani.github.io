{
  "name": "azzindani-portfolio",
  "version": "0.0.0",
  "private": true,
  "description": "Personal portfolio site (static, no runtime build).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps chromium",
    "check": "npm test && npm run e2e",
    "serve": "node tests/serve.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "vitest": "^1.6.0"
  }
}
